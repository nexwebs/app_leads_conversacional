# Permitir conexiones locales sin contraseña (dentro del contenedor)
local   all             all                                     trust

# Permitir conexiones desde Docker (red interna)
host    all             all             172.16.0.0/12           md5

# ✅ NUEVO: Permitir conexiones desde tu PC local
host    all             all             0.0.0.0/0               md5