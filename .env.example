# ENTORNO
APP_ENV=development
APP_NAME="Agent AI LeadsProfile"
LOG_LEVEL=INFO

# DOMINIO (Cambiar en producción)
DOMAIN=localhost
USE_SSL=false


# BASE DE DATOS
POSTGRES_USER=postgres
POSTGRES_PASSWORD=adminp
POSTGRES_DB=crm_leadsdb

# Desarrollo local
#DATABASE_URL=postgresql+asyncpg://postgres:adminsa007@localhost:5432/crm_leadsdb

# Docker (descomentar si usas Docker)
DATABASE_URL=postgresql+asyncpg://postgres:adminp@localhost:5432/crm_leadsdb
# OPENAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
EMBEDDING_MODEL=text-embedding-3-small

# SEGURIDAD (IMPORTANTE)
# SECRET_KEY - Generar con: openssl rand -hex 32
# NUNCA uses el valor por defecto en producción
SECRET_KEY=CAMBIAR-EN-PRODUCCION-CON-COMANDO-OPENSSL

# JWT Expiration (30 minutos recomendado)
ACCESS_TOKEN_EXPIRE_MINUTES=30


# RATE LIMITING (Ajustar según tu tráfico)
# Endpoints públicos (chat, leads POST)
RATE_LIMIT_PUBLIC_RPM=10
RATE_LIMIT_PUBLIC_RPH=100

# Endpoints de autenticación
RATE_LIMIT_AUTH_RPM=5
RATE_LIMIT_AUTH_RPH=20

# WebSocket
RATE_LIMIT_WS_RPM=30
RATE_LIMIT_WS_RPH=500


# CORS (Actualizar con tus dominios reales)
# En desarrollo: localhost
# En producción: tus dominios sin wildcard (*)
CORS_ORIGIN_1=http://localhost:4321
CORS_ORIGIN_2=
# CORS_ORIGIN_3=https://tu-dominio.com
# CORS_ORIGIN_4=https://www.tu-dominio.com


# EMAIL

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_VENTAS=claudio201394@gmail.com
EMAIL_SOPORTE=clblommberg@gmail.com


# EMPRESA
NOMBRE_EMPRESA="NexWebs"
SITIO_WEB=https://nextwebs.soy.pe
TELEFONO_SOPORTE="+51 907 321 211"

# GOOGLE OAUTH (opcional)
# Si usas client_service.json, déjalo vacío
# Si quieres usar variable de entorno, pega el JSON aquí
GOOGLE_CLIENT_CONFIG=


# LÍMITES DEL SISTEMA (512MB RAM)

DB_POOL_SIZE=1
DB_MAX_OVERFLOW=1
MAX_CONCURRENT_REQUESTS=2
MAX_WEBSOCKET_CONNECTIONS=3
EMBEDDING_CACHE_SIZE=10


# NOTAS IMPORTANTES


# 1. SECRET_KEY: NUNCA commitear la real al repositorio
# 2. CORS: En producción, especificar dominios exactos
# 3. Rate Limits: Ajustar según análisis de tráfico
# 4. USE_SSL: true si usas HTTPS en producción
# 5. APP_ENV: production al desplegar

# Generar SECRET_KEY:
# openssl rand -hex 32

# Validar configuración:
# python validate_security.py
